# frontEND (Vue)

Planned:
- Vue (Vite) UI for:
  - Ask questions (/ask)
  - Character page (/character/{name})
  - Graph visualization (/graph/{character})

This folder is reserved to keep a clean mono-repo structure.